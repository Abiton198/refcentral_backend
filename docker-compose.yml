services:
  refcentral-api:
    build: .
    container_name: refcentral-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./secrets:/app/secrets:ro
    restart: unless-stopped
